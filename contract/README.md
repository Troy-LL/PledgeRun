# PledgeRun — Soroban contract

This crate is the **PledgeRun** escrow contract (`pledge_run`). Build, test, and deploy from here.

See the [repository README](../README.md) for prerequisites, `stellar contract build`, `cargo test`, and testnet deploy steps.
