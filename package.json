{
  "name": "pledgerun",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/Troy-LL/PledgeRun.git"
  },
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "build:web": "npm run build --prefix web",
    "preview:web": "npm run preview --prefix web"
  }
}
